using StatsPlots
gr(size=(400,300))
# pyplot(size = (400, 300))

# histogram(randn(1000))

# pygui(true)
display(plot(histogram(randn(1000))))
# gui()

# ctrl+shift+p --> Start REPL
# 或
# shift + enter   # OK,  在repl里执行
